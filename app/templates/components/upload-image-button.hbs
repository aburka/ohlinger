{{#if value.url}}
  <img class="upload-image-button-thumbnail" src="{{value.url}}" alt="">
{{/if}}
<button class="btn" disabled={{isUploading}} {{action "uploadImage"}}>
  {{#if isUploading}}
    Uploading...
  {{else}}
    Upload new image
  {{/if}}
</button>

<input type="file" class="upload-image-button-input">
