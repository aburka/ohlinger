<div class="row">
{{#gallery-modal images=model as |openGallery|}}
  {{#masonry-grid}}
    {{#each model as |img index|}}
      <div class="col-xs-12 col-sm-3 col-md-4">
        <div class="image-index-thumbnail thumbnail">
          <img src="{{img.image.url}}" alt="" {{action openGallery index}} />
          <div class="caption">
            {{#link-to 'art.show' img.objectId}}
              <h4><strong>{{img.title}}</strong></h4>
            {{/link-to}}
          </div>
        </div>
      </div>
    {{/each}}
  {{/masonry-grid}}
{{/gallery-modal}}
</div>
