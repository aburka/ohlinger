<div class="row">
{{#gallery-modal images=model as |openGallery|}}
  {{#masonry-grid}}
    {{#each model as |product index|}}
      {{store-item product=product index=index openGallery=openGallery}}
    {{/each}}
  {{/masonry-grid}}
{{/gallery-modal}}
</div>

{{yield}}
