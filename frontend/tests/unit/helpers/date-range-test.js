import { dateRange } from '../../../helpers/date-range';
import { module, test } from 'qunit';

module('Unit | Helper | date range');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dateRange(42);
  assert.ok(result);
});
