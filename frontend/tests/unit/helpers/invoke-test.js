import { invoke } from '../../../helpers/invoke';
import { module, test } from 'qunit';

module('Unit | Helper | invoke');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = invoke(42);
  assert.ok(result);
});
